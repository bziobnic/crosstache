# App UX modernization implementation evidence

This is an implementation-evidence ledger, not a release sign-off. The ranges
begin at `e6f70b1` and end at `04a8cf8`. Source paths and commit ranges below
are tracked production/test evidence; historical browser and host results are
explicitly labelled. The final automated repository and host gates pass.

| Item | Production files | Red test observed | Passing tests | Runtime evidence | Accessibility/responsive evidence |
| ---: | --- | --- | --- | --- | --- |
| 1 | `src/web/assets/dialogs.js`, `secrets.js`, `ui-model.js` (`e6f70b1..5715b04`) | `dialogs.test.js: navigation guard keeps a dirty draft unless discard is confirmed` was RED before the guard. | `node --test src/web/assets/dialogs.test.js src/web/assets/secrets.routes.test.js` — 9 passed. | Historical browser flow exercised dirty Escape, Keep editing, Discard, and invoker restoration; the fresh aggregate Playwright gate passed. | `tests/web/ui-accessibility.spec.js` — 2 passed for inert background, focus, nested confirmation, and restoration. |
| 2 | `src/web/assets/dialogs.js`, `accessibility.js`, `index.html`, `style.css` (`49f92d4..4ab16b5`) | `dialogs.test.js: modal manager skips CSS-hidden focusables` was RED before hidden controls were excluded. | Historical `npx playwright test` — 9 Playwright/axe tests passed. | Historical browser sheet flow exercised initial focus, containment, Escape, and restoration; the fresh keyboard-capable Playwright gate passed. | Serious/critical axe findings were absent; protected controls are outside labels with field-specific state/timer relationships. |
| 3 | `src/web/secrets.rs`, `src/web/assets/secrets.js`, `index.html`, `style.css` (`d4e88ac..378ccc5`; evidence correction `971cf67`) | `GET /api/secrets/deleted` returned 404 (expected 200) before the Trash route. | `cargo test --features ui web::secrets::tests --lib` — 5 passed; `npm run test:unit` — 40 passed; workspace purge routing — 91/91 passed. | `tests/web/ui-trash.spec.js` exercised delete, Undo, restore, conflict, purge, and file recovery warning; the fresh aggregate Playwright gate passed. | Trash flow was included in axe coverage; Undo is inert behind modals. |
| 4 | `src/web/preferences.rs`, `src/web/assets/preferences.js`, `secrets.js`, `accessibility.js` (`378ccc5..6afbe5c`) | `secrets.test.js: clipboard clearing never overwrites a newer value` was RED before ownership checking. | Historical `node --test src/web/assets/*.test.js` — 60 passed; Playwright — 8 passed. Corrected timer boundary passed 20/20 on the host. | `tests/web/ui-protected-values.spec.js` exercised exact 29s/reset/29s/1s timing, blur/hide/close, and unchanged-clipboard clearing; the fresh aggregate Playwright gate passed. | Final axe coverage had no serious/critical findings; controls expose field-specific state/timer relationships. |
| 5 | `desktop/src-tauri/src/startup.rs`, `desktop/frontend/loading.js`, `loading.css` (`21794f6..23316a9`) | `desktop/frontend/loading.test.js` was RED with `ERR_MODULE_NOT_FOUND`; package request test was RED before `PackageSmokeEnvironment`. | `node --test desktop/frontend/loading.test.js` — 9 passed; `node tests/desktop/startup-smoke.js` — 13 passed; `cargo test -p xv-desktop` — 23 passed. | Fresh exact default `bash tests/desktop/package-smoke.sh` completed its release build and unsigned isolated Local bundle in 2m56s with `package smoke passed`. Invalid Azure/AWS are covered by mounted/renderer recovery-state tests, not fresh packaged launches. | `tests/desktop/startup-browser.spec.js` historical Playwright/axe result: 2 passed, zero violations; Task 4 visual review covered desktop and 390px. |
| 6 | `src/web/context.rs`, `api.rs`, `assets/context.js`, `assets/app.js` (`4ab16b5..63f4de5`) | `desktop_raw_config_folds_project_backend_before_workspace_and_route_state` was RED (`azure` vs `local`). | `cargo test --features ui web::context::tests --lib` — 10 passed; final focused JavaScript set — 75 passed. | Mounted context flow exercised exact alias/backend/vault routing and switch locking; the fresh aggregate Playwright gate passed. | `tests/web/ui-context.spec.js` covers keyboard Commands/Help/Settings and persistent Settings Retry. |
| 7 | `src/web/assets/ui-model.js`, `secrets.js`, `files.js`, `style.css` (`63f4de5..346793c`) | Required model tests were RED before `buildFolderTree`, `initialExpansion`, and `folderPreferenceKey`. | `node --test src/web/assets/*.test.js` — 105 passed; `npx playwright test tests/web/ui-folders.spec.js` — 6 passed. | `tests/web/ui-folders.spec.js` exercised descendant filtering, scoped expansion, and stale Files suppression; the fresh aggregate Playwright gate passed. | Semantic `tree`/`treeitem` roving-keyboard coverage ran desktop/mobile; historical full browser suite — 23 passed including axe. |
| 8 | `src/web/assets/typed-editor.test.js`, `secrets.js`, `ui-model.js`, `style.css` (`bb5b09c..1ff56d1`) | `typed-editor.test.js` failed because `buildTypedDraft` was not exported. | `node --test src/web/assets/*.test.js` — 110 passed; `npx playwright test tests/web/ui-typed-editor.spec.js` — 15 passed. | Browser flow covered cards, conversion/rename, chips, suggestions, folder autocomplete, and date/no-expiry; the fresh aggregate Playwright gate passed. | Accessible type cards and selected fields have recorded responsive axe-clean coverage. |
| 9 | `src/web/errors.rs`, `api.rs`, `assets/api-client.js`, `secrets.js` (`346793c..a782c02`) | `web::api::tests::missing_secret_has_stable_error` was RED because `error.code` was null. | Historical JavaScript set — 124 passed; `cargo test --features ui web:: --lib` — 143 passed. | `tests/web/ui-errors.spec.js` exercised durable Retry/Copy details, failed-only retry, and late-result suppression; the fresh aggregate Playwright gate passed. | Persistent error alerts expose keyboard-reachable Retry/Copy details. |
| 10 | `src/web/assets/commands.js`, `store.js`, `secrets.js`, `files.js` (`a782c02..3ad1f2e`) | Shortcut/palette contracts were RED before the command model and owner guards. | Historical JavaScript set — 149 passed; `npx playwright test` — 54 passed. | `tests/web/ui-commands.spec.js` exercised Cmd/Ctrl+K, `/`, Cmd/Ctrl+N, Escape, find/switch/action, and stale guards; the fresh aggregate Playwright gate passed. | Palette uses input-owned `aria-activedescendant`; historical browser result includes narrow responsive accessibility. |
| 11 | `src/web/assets/files.js`, `api-client.js`, `style.css`, `src/web/files.rs` (`3ad1f2e..e7479fc`) | Queue tests were RED before XHR progress/cancel state and conflict decisions. | `node --test src/web/assets/*.test.js` — 171 passed; `npx playwright test` — 21 upload/mobile/axe scenarios passed. | `tests/web/ui-uploads.spec.js` exercised progress, cancel, retry, conflicts, destination, summary, and navigation guard; the fresh aggregate Playwright gate passed. | Keyed queue rows retain focus; the 21-scenario run included live/mobile accessibility. |
| 12 | `src/web/assets/ui-model.js`, `secrets.js`, `files.js`, `style.css`, `desktop/src-tauri/src/main.rs` (`e7479fc..21794f6`; evidence refresh `567d918`) | `ui-model.test.js: content mode changes at the approved breakpoint` was RED before `contentMode(768)` and stacked rows. | `node --test src/web/assets/*.test.js` — 179 passed; refreshed visual gate — 8/8 passed on the host. | All eight refreshed light/dark screenshots were independently inspected at 1180×760, 820×560, 768×700, and 390×844. | `tests/web/ui-responsive.spec.js` preserves full identifiers; visual gate found no serious/critical axe issue or horizontal overflow. |
| 13 | `src/web/assets/accessibility.js`, `secrets.js`, `files.js`, `index.html`, `style.css` (`b3af71c..e763538`) | `accessibility.test.js: mountTabs activates focused tabs with arrows and Home/End` was RED before the helper. | `node --test src/web/assets/*.test.js` — 179 passed; `npx playwright test` — 93 passed. | `tests/web/ui-navigation.spec.js` exercised tab/panel/tree/Escape, selection, capability-loss cleanup, and responsive focus; the fresh aggregate Playwright gate passed. | Helpers enforce one-control rows and responsive focus mapping; historical browser run includes responsive accessibility. |
| 14 | `src/web/assets/settings.js`, `preferences.js`, `context.js`, `style.css` (`20362f9..03483e2`) | `settings.test.js` was RED with `ERR_MODULE_NOT_FOUND`; unsafe diagnostics and timeout labels also had recorded RED regressions. | `npm run test:unit` — 194 passed; Settings/Help Playwright/axe — 4 passed; functional Playwright — 98 passed. | Production-mounted Settings/Help checks covered theme, persisted density/timeout/reset, redacted diagnostics, responsive sheet, and Close hit-testing; the fresh aggregate Playwright gate passed. | The focused 4-test browser run covered axe, focus restoration, 390px geometry, and reduced motion. |

## Finalization status

The matrix has fourteen populated evidence rows and the 29-task modernization
backlog is complete. After the test-harness compatibility corrections in
`c2bcc00`, `971cf67`, and `04a8cf8`, the exact host
`cargo test --all-features` gate exited 0: library and binary targets each
reported 1240 passed/1 ignored, AWS mocks passed 32/32, clipboard passed 9/9,
workspace tests passed 91/91, gen bounds passed 2/2 with one live test ignored,
doc tests passed 2/2, and all remaining suites passed with only their
documented ignores. The library/binary ignored test is
`cli::upgrade_ops::tests::test_fetch_latest_release_from_github`; the gen
ignore requires live Azure credentials.

The corrected protected-value timer passed 20/20 on the host, all eight
refreshed visual snapshots passed and were independently inspected, and the
fresh full Playwright gate passed 106/106 in 2.8 minutes. The exact default
package smoke also passed after building and launching the unsigned isolated
Local bundle. Invalid Azure/AWS recovery has fresh mounted/renderer automated
coverage, but no fresh packaged invalid-cloud launch is claimed. The fresh
106-test Playwright gate supplies the final keyboard/responsive runtime
coverage, while the inspected eight-snapshot gate supplies the four-viewport
light/dark evidence.
