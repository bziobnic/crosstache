# Security Review

Repository-wide review of revision `98a39ff9266d08d001dc4851b4eb414a8a7d762b`. All 175 in-scope files received full-file review receipts. Final policy calibration produced 15 High, 32 Medium, and 2 Low findings; one dependency-reachability question remains deferred.

## P1 - High

- **Deployment archive follows repository symlinks and uploads outside files** (`xfunction/installer/steps/deployment.py:25`) - A lower-trust input or workflow state reaches the local file disclosure control at xfunction/installer/steps/deployment.py:25 under the validated preconditions. Fix: Reject symlinks and require realpath containment for every archive entry.
- **Dotenv export permits shell command substitution in secret values** (`src/cli/config_ops.rs:2447`) - A lower-trust input or workflow state reaches the command injection control at src/cli/config_ops.rs:2447 under the validated preconditions. Fix: Use POSIX-safe single quoting and validate assignment names, or document a non-shell dotenv grammar.
- **Existing Function App adoption exposes a newly privileged client secret** (`xfunction/installer/steps/function_app.py:12`) - A lower-trust input or workflow state reaches the credential exposure control at xfunction/installer/steps/function_app.py:12 under the validated preconditions. Fix: Create a new app or require verified installer ownership before writing credentials.
- **File sync follows symlink components outside the selected root** (`src/cli/file_ops.rs:1755`) - A lower-trust input or workflow state reaches the symlink confinement control at src/cli/file_ops.rs:1755 under the validated preconditions. Fix: Use directory-handle-relative no-follow writes and reject symlink components.
- **Mutable CreatedByID tag can escalate a vault manager to Owner** (`xfunction/function_app.py:383`) - A lower-trust input or workflow state reaches the privilege escalation control at xfunction/function_app.py:383 under the validated preconditions. Fix: Authorize creator status from immutable deployment provenance or existing RBAC authority, not mutable tags.
- **Mutable release actions execute with signing and repository-write authority** (`.github/workflows/release.yml:8`) - A lower-trust input or workflow state reaches the CI supply chain control at .github/workflows/release.yml:8 under the validated preconditions. Fix: Pin every action to a reviewed commit SHA and scope permissions per job.
- **Preclaimed app registration receives subscription-wide administrative roles** (`xfunction/installer/steps/app_registration.py:10`) - A lower-trust input or workflow state reaches the privilege escalation control at xfunction/installer/steps/app_registration.py:10 under the validated preconditions. Fix: Require a persisted application object ID and verified ownership; never adopt by display name alone.
- **Recursive downloads follow attacker-planted symlink components** (`src/cli/file_ops.rs:1532`) - A lower-trust input or workflow state reaches the symlink confinement control at src/cli/file_ops.rs:1532 under the validated preconditions. Fix: Apply no-follow containment to every directory component and final file.
- **Release job executes an unverified signer with the private signing key** (`.github/workflows/release.yml:126`) - A lower-trust input or workflow state reaches the CI supply chain control at .github/workflows/release.yml:126 under the validated preconditions. Fix: Pin signer archives by independently verified digest or provenance before execution.
- **Secret-level revoke deletes broader inherited Azure role assignments** (`src/vault/operations.rs:847`) - A lower-trust input or workflow state reaches the authorization integrity control at src/vault/operations.rs:847 under the validated preconditions. Fix: Filter and verify every returned assignment has the exact target secret scope.
- **Single and multi-file downloads follow output-tree symlinks** (`src/cli/file_ops.rs:579`) - A lower-trust input or workflow state reaches the symlink confinement control at src/cli/file_ops.rs:579 under the validated preconditions. Fix: Open destinations relative to a trusted directory without following links.
- **Substring storage discovery grants roles on unrelated accounts** (`xfunction/StorageRoleManager/storage_role_manager.py:115`) - A lower-trust input or workflow state reaches the privilege escalation control at xfunction/StorageRoleManager/storage_role_manager.py:115 under the validated preconditions. Fix: Bind storage accounts with exact resource IDs or immutable relationship metadata.
- **Unix installer accepts unsigned release archives** (`scripts/install.sh:201`) - A lower-trust input or workflow state reaches the release authenticity control at scripts/install.sh:201 under the validated preconditions. Fix: Verify the published minisign signature with an embedded public key before extraction.
- **Windows drive and UNC blob names escape the sync root** (`src/blob/sync.rs:98`) - A lower-trust input or workflow state reaches the path traversal control at src/blob/sync.rs:98 under the validated preconditions. Fix: Reject Prefix and RootDir components and require target containment before writes.
- **Windows installer accepts unsigned release archives** (`scripts/install.ps1:348`) - A lower-trust input or workflow state reaches the release authenticity control at scripts/install.ps1:348 under the validated preconditions. Fix: Verify the published minisign signature before extraction and installation.

## P2 - Medium

- **Attacker-authored issue context can prompt-inject a privileged coding agent** (`.github/workflows/claude.yml:53`) - A lower-trust input or workflow state reaches the agent/tool boundary control at .github/workflows/claude.yml:53 under the validated preconditions. Fix: Constrain tools, isolate untrusted context, and require approval for shell/write actions.
- **Build workflow executes mutable third-party action tags** (`.github/workflows/build.yml:19`) - A lower-trust input or workflow state reaches the CI supply chain control at .github/workflows/build.yml:19 under the validated preconditions. Fix: Pin third-party actions to immutable reviewed commit SHAs.
- **Checkout-controlled .xv directory redirects local context writes** (`src/config/context.rs:215`) - A lower-trust input or workflow state reaches the symlink confinement control at src/config/context.rs:215 under the validated preconditions. Fix: Validate every parent component or write through a confined directory handle.
- **Context-store load errors silently select a fallback workspace** (`src/workspace/mod.rs:280`) - A lower-trust input or workflow state reaches the configuration trust control at src/workspace/mod.rs:280 under the validated preconditions. Fix: Fail closed on context load errors instead of replacing them with defaults.
- **Default-vault collisions redirect explicitly qualified mutations** (`src/workspace/resolve.rs:101`) - A lower-trust input or workflow state reaches the name resolution control at src/workspace/resolve.rs:101 under the validated preconditions. Fix: Resolve explicit aliases before default-vault literal collision checks.
- **Deployment script reports success from a non-project directory** (`xfunction/scripts/deploy-function.ps1:34`) - A lower-trust input or workflow state reaches the deployment integrity control at xfunction/scripts/deploy-function.ps1:34 under the validated preconditions. Fix: Use the project root and fail on every native command error before success output.
- **Deployment verifier increments the wrong PowerShell scope and exits zero** (`xfunction/scripts/verify-deployment.ps1:34`) - A lower-trust input or workflow state reaches the deployment integrity control at xfunction/scripts/verify-deployment.ps1:34 under the validated preconditions. Fix: Use script-scoped counters and assert every required role, scope, endpoint, and filter.
- **Freshly installed Graph module receives directory-wide write scopes** (`xfunction/scripts/configure-graph-permissions.ps1:62`) - A lower-trust input or workflow state reaches the supply chain privilege control at xfunction/scripts/configure-graph-permissions.ps1:62 under the validated preconditions. Fix: Pin the module and request only the least Graph scopes required.
- **Function logs validated identity claims and complete vault metadata** (`xfunction/function_app.py:284`) - A lower-trust input or workflow state reaches the sensitive logging control at xfunction/function_app.py:284 under the validated preconditions. Fix: Use an allowlist of non-sensitive operational fields and redact tags and claims.
- **Function principal receives subscription-wide Key Vault Administrator** (`xfunction/installer/steps/rbac.py:8`) - A lower-trust input or workflow state reaches the excessive privilege control at xfunction/installer/steps/rbac.py:8 under the validated preconditions. Fix: Replace the built-in role with a least-privilege management-plane custom role at narrow scope.
- **Function principal receives unconstrained subscription-wide RBAC administration** (`xfunction/installer/steps/rbac.py:7`) - A lower-trust input or workflow state reaches the excessive privilege control at xfunction/installer/steps/rbac.py:7 under the validated preconditions. Fix: Use a conditioned custom role limited to required roles, principals, and resource scopes.
- **Installer exposes a privileged client secret in child-process arguments** (`xfunction/installer/steps/function_app.py:41`) - A lower-trust input or workflow state reaches the credential exposure control at xfunction/installer/steps/function_app.py:41 under the validated preconditions. Fix: Use a protected file or stdin-based secret transfer and redact subprocess errors.
- **Installer state writes follow checkout-controlled symlinks** (`xfunction/installer/config.py:54`) - A lower-trust input or workflow state reaches the symlink confinement control at xfunction/installer/config.py:54 under the validated preconditions. Fix: Use exclusive no-follow atomic state writes with private permissions.
- **Literal-name probing overrides explicit workspace aliases on reads** (`src/workspace/resolve.rs:153`) - A lower-trust input or workflow state reaches the name resolution control at src/workspace/resolve.rs:153 under the validated preconditions. Fix: Treat a valid explicit alias as authoritative before literal cross-vault probing.
- **Malformed project config silently falls back to a trusted global vault** (`src/config/settings.rs:466`) - A lower-trust input or workflow state reaches the configuration trust control at src/config/settings.rs:466 under the validated preconditions. Fix: Propagate discovered project-config read and parse errors before side effects.
- **Manual client-secret entry is echoed to the terminal** (`xfunction/installer/utils/prompts.py:9`) - A lower-trust input or workflow state reaches the credential exposure control at xfunction/installer/utils/prompts.py:9 under the validated preconditions. Fix: Use getpass or an equivalent no-echo prompt.
- **Migration treats target existence-check errors as absence** (`src/cli/migrate_ops.rs:69`) - A lower-trust input or workflow state reaches the data integrity control at src/cli/migrate_ops.rs:69 under the validated preconditions. Fix: Fail closed on unknown target state and use conditional create/update operations.
- **Mutable Claude action runs with API, OIDC, and repository credentials** (`.github/workflows/claude.yml:35`) - A lower-trust input or workflow state reaches the CI supply chain control at .github/workflows/claude.yml:35 under the validated preconditions. Fix: Pin the action to a reviewed SHA and minimize credentials and permissions.
- **Non-UTF-8 staged filenames are skipped by the leak scanner** (`src/scan/staged.rs:25`) - A lower-trust input or workflow state reaches the security-control bypass control at src/scan/staged.rs:25 under the validated preconditions. Fix: Preserve raw Git path bytes and fail loud on unreadable staged objects.
- **Non-atomic Event Grid updates can leave a privileged trigger unfiltered** (`xfunction/scripts/update-event-grid-filter.ps1:52`) - A lower-trust input or workflow state reaches the deployment integrity control at xfunction/scripts/update-event-grid-filter.ps1:52 under the validated preconditions. Fix: Apply an atomic desired configuration or rollback and verify exact final filters.
- **Project configuration updates follow malicious .xv.toml symlinks** (`src/config/project.rs:203`) - A lower-trust input or workflow state reaches the symlink confinement control at src/config/project.rs:203 under the validated preconditions. Fix: Use no-follow atomic writes and reject symlinked project config files.
- **Project record types can downgrade secret fields into listable metadata** (`src/records/types.rs:276`) - A lower-trust input or workflow state reaches the sensitive storage control at src/records/types.rs:276 under the validated preconditions. Fix: Prevent secret-to-metadata overrides of built-ins without an explicit trust decision.
- **Repository-controlled scan policy can disable its own security gate** (`src/cli/scan_ops.rs:57`) - A lower-trust input or workflow state reaches the security-control bypass control at src/cli/scan_ops.rs:57 under the validated preconditions. Fix: Apply a trusted hook/CI baseline that repository policy cannot weaken.
- **Secret-name normalization collisions inject the wrong child environment value** (`src/cli/secret_ops.rs:5765`) - A lower-trust input or workflow state reaches the secret substitution control at src/cli/secret_ops.rs:5765 under the validated preconditions. Fix: Detect derived environment-name collisions and require explicit resolution.
- **Static tags select and delete unrelated storage accounts** (`xfunction/installer/steps/storage_account.py:15`) - A lower-trust input or workflow state reaches the destructive authorization control at xfunction/installer/steps/storage_account.py:15 under the validated preconditions. Fix: Use a unique deployment ID and exact persisted resource ID for adoption and teardown.
- **Teardown erases recovery state after privileged cleanup failures** (`xfunction/installer/steps/teardown.py:102`) - A lower-trust input or workflow state reaches the cleanup integrity control at xfunction/installer/steps/teardown.py:102 under the validated preconditions. Fix: Retain state and return failure until every privileged resource and credential is removed.
- **Uninstall deletes pre-existing Azure resources it did not create** (`xfunction/installer/steps/resource_group.py:20`) - A lower-trust input or workflow state reaches the destructive authorization control at xfunction/installer/steps/resource_group.py:20 under the validated preconditions. Fix: Persist creation provenance and delete only exact installer-owned resources.
- **Uninstall removes role assignments that predated installation** (`xfunction/installer/steps/rbac.py:53`) - A lower-trust input or workflow state reaches the destructive authorization control at xfunction/installer/steps/rbac.py:53 under the validated preconditions. Fix: Persist assignment IDs and delete only assignments created by this run.
- **Unused Function managed identity receives subscription-wide administration** (`xfunction/scripts/setup-managed-identity.ps1:73`) - A lower-trust input or workflow state reaches the excessive privilege control at xfunction/scripts/setup-managed-identity.ps1:73 under the validated preconditions. Fix: Remove unused assignments or scope a required identity to exact resources and actions.
- **Updater accepts replayed older signed binaries under a newer tag** (`src/cli/upgrade_ops.rs:137`) - A lower-trust input or workflow state reaches the rollback protection control at src/cli/upgrade_ops.rs:137 under the validated preconditions. Fix: Bind the signature to version, asset name, and hash and verify embedded binary version.
- **Vault RBAC commands mutate ignored legacy access policies** (`src/vault/operations.rs:646`) - A lower-trust input or workflow state reaches the authorization integrity control at src/vault/operations.rs:646 under the validated preconditions. Fix: Use Microsoft.Authorization role assignments at exact vault scope.
- **Vault read failures silently disable exact secret-value scanning** (`src/scan/orchestrator.rs:98`) - A lower-trust input or workflow state reaches the security-control bypass control at src/scan/orchestrator.rs:98 under the validated preconditions. Fix: Fail hook mode when requested vault coverage is incomplete and report failed values.

## P3 - Low

- **CSV exports preserve spreadsheet formulas from secret values** (`src/cli/config_ops.rs:2442`) - A lower-trust input or workflow state reaches the formula injection control at src/cli/config_ops.rs:2442 under the validated preconditions. Fix: Neutralize formula prefixes and use a structured CSV writer for every field.
- **Unrelated account activity can hide vault audit events** (`src/backend/aws/audit.rs:63`) - A lower-trust input or workflow state reaches the audit integrity control at src/backend/aws/audit.rs:63 under the validated preconditions. Fix: Filter at the service when possible and page until the requested time window is covered.

## Needs Follow-up

- **quick-xml advisory reachability** (`Cargo.lock:4530`) - `quick-xml 0.31.0` is in the active Azure SDK dependency graph and matches RUSTSEC-2026-0194, but the review did not establish whether an attacker can induce an attribute-dense Azure XML response.

## Coverage And Limitations

- Included Rust runtime code, Azure Function code, embedded web assets, installers, PowerShell deployment scripts, GitHub Actions workflows, `Cargo.lock`, and Python requirements.
- Excluded ignored duplicate worktrees and local agent state under `.beads/`, `.claude/`, and `.omc/`; docs and tests were supporting evidence.
- No live Azure or AWS account was used for destructive or privilege-changing reproduction.
- `cargo-audit` and `cargo-deny` were not installed; the lockfile received manual/advisory review.
- The full sealed scan report and evidence bundle are stored in the scan artifact directory referenced in the session handoff.
